import { Audiowide, Slackey, DM_Sans } from 'next/font/google'

export const dmsans = DM_Sans({
  subsets: ['latin'],
  display: 'swap',
})

export const audiowide = Audiowide({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
})

export const slackey = Slackey({
  subsets: ['latin'],
  weight: '400',
  display: 'swap',
})
